# Configurations

This directory contains configuration files, examples and templates for the AgentPress system.

## Contents
- `example_config.json` - Example configuration values for reference
- Other config templates as needed

Configuration files should be customized according to your specific setup requirements.

**Note**: Actual API keys and environment-specific configurations should be stored in `.env` file, not committed to version control.